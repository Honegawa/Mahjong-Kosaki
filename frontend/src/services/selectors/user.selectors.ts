import { RootState as RootStateArticle } from "../../interfaces/user";

export const allUsers = (state: RootStateArticle) => state.users.data;

// export const oneUser = (state: RootStateArticle) => state.users.data;
